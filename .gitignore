# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
# monorepo（Step 2.1，ADR-0022）：pnpm 在每个 apps/*、packages/* 子包下都会生成
# 自己的 node_modules（内容是指向根 node_modules/.pnpm 仓库的符号链接）。
# 原来的 `/node_modules` 只忽略根目录一层，改成不带前导 `/` 以匹配所有层级。
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
packages/db/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# 测试和调试脚本（不部署）
test-*.ts
view-*.ts
